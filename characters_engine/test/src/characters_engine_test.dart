// ignore_for_file: prefer_const_constructors
import 'package:characters_engine/characters_engine.dart';
import 'package:test/test.dart';

void main() {
  group('CharactersEngine', () {
    test('can be instantiated', () {
      expect(CharactersEngine(), isNotNull);
    });
  });
}
